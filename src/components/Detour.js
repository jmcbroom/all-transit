import React from "react";
import { Message, Segment, Icon, Header } from "semantic-ui-react";

export const Detour = () => (
  <Segment basic>
    <Header color='red' as="h5" content="Detours in effect" icon="warning sign" />
  </Segment>
);
